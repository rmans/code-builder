// Variant B: Enhanced version
// Variant B: Enhanced version
/**
 * Returns a greeting.
 * @param name Optional name to greet.
 * @returns Greeting string.
 */
export const hello = (name?: string): string => (name ? `hi, ${name}` : 'hi');
